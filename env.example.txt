# Next auth https://next-auth.js.org/configuration/options
# Run this command to generate a a new NEXTAUTH_SECRET
# $ openssl rand -base64 32

# Frontend runs on port 3001, backend API runs on port 3000
NEXTAUTH_URL=http://localhost:3001
NEXTAUTH_SECRET=your-secret-key-replace-with-secure-random-string

# Backend API URL - This is where your ezlinkai backend is running
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Go to github and setup the oauth configuration (optional)
# https://next-auth.js.org/providers/github#configuration
# https://github.com/settings/developers
GITHUB_ID=your-github-client-id
GITHUB_SECRET=your-github-client-secret

# Google OAuth configuration (optional)
GOOGLE_ID=your-google-client-id
GOOGLE_SECRET=your-google-client-secret



